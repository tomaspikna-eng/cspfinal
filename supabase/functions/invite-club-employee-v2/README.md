# invite-club-employee-v2

Deployed Supabase Edge Function for Club Manager employee account activation. The deployed runtime validates the current manager, sends the Supabase Auth invitation or activation resend, and links `club_employees.auth_user_id`.
