[package]
name = "csp-windows"
version = "0.1.0"
description = "Connect Sports Pro desktop client for Windows"
authors = ["Connect Sports Pro"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
